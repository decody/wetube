extends layouts/main
include mixins/videoBlock

block content
    .home-videos
        p Video List in Homepage 
        each item in videos
            +videoBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                videoFile:item.fileUrl
            })